Чтобы запустить проект нужно написать следующий команды в терминале:
cd client
npm run start
cd ../
cd server
npm run server

При первоначальной загрузке проекта в github для каждой папки нужно прописать:
npm i
